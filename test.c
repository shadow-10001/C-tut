#include <stdio.h>
int main()
{
    int n, a;
    n = 100;
    a = 50;
    int diff;
    diff = n - a;
    printf("The difference of two numbers is %d\n", diff);
    // hello
    int x, y;
    x = 2;
    y = 10;
    int multiply;
    multiply = x * y;
    printf("After multiplying two numbers we get %d\n", multiply);
    int f;
    n = 500;
    f = 200;
    int sum;
    sum = n + f;
    printf("The sum of two numbers is %d", sum);

    return 0;
}
